# Đúc Đồng Lộc Nam — Design System & Visual Authority (Impeccable Craft)

## 1. Triết lý Thiết kế (Design Philosophy)
- **Phong cách:** *Quiet Traditional Luxury & Sacred Vietnamese Heritage* (Sang trọng tĩnh lặng, đậm hồn cốt văn hóa tâm linh truyền thống).
- **Nguyên tắc thẩm mỹ:**
  - Tương phản tinh tế: Nền kem sáng ấm áp (#FBF6EC) làm nổi bật chất kim loại đồng cổ và sắc chữ nâu gỗ đậm (#5A3A22).
  - Điểm nhấn quyền quý: Điểm xuyết viền hoàng kim đồng (#C9A15A) và nút hành động đỏ rượu bordeaux (#7B1E2B).
  - Chi tiết thủ công: Hoa văn trống đồng, vân mây và đường viền trang trí nhẹ nhàng, trang nhã.

## 2. Bảng Màu Thương Hiệu (Color Tokens)
- **Primary Background (Nền chính):** `#FBF6EC` (`bg-kem-light`) — Kem sáng thanh nhã.
- **Secondary Background (Nền phụ/Card):** `#F3E9D2` (`bg-kem-subtle`) / `#FFFFFF`
- **Primary Text (Chữ chính):** `#5A3A22` (`text-bronze-dark`) — Nâu đậm gỗ gụ.
- **Heading Text (Tiêu đề):** `#7A4A1F` (`text-bronze-accent`) — Nâu ánh đồng cổ.
- **Metallic Border / Ornament (Viền đồng):** `#C9A15A` (`border-bronze-gold`) — Vàng đồng ánh kim.
- **Primary CTA (Nút hành động chính):** `#7B1E2B` (`bg-bordeaux`), Hover: `#611722` (`bg-bordeaux-hover`).
- **Secondary Button (Nút phụ):** Nền trong suốt, viền nâu `#5A3A22`, chữ nâu `#5A3A22`.

## 3. Typography
- **Headings (Tiêu đề):** `Noto Serif`, `Playfair Display`, `serif` (Trang nghiêm, quyền quý, cổ điển).
- **Body & Controls (Nội dung):** `Inter`, `Be Vietnam Pro`, `sans-serif` (Rõ ràng, hỗ trợ tiếng Việt trọn vẹn, dễ đọc ở mọi kích thước).

## 4. Hierarchy & Interaction
- **Card:** Viền mỏng `border-bronze-gold/30`, bo góc nhẹ `rounded-sm`, hiệu ứng nâng nhẹ khi hover (`hover-lift`).
- **Nút CTA:** Bo góc nhẹ `rounded`, đổ bóng `shadow-bordeaux`, hiệu ứng chuyển tiếp êm ái (`transition-all duration-300`).
- **Floating Contact:** Nút Zalo & Hotline cố định góc dưới màn hình có hiệu ứng pulse nhẹ thu hút chuyển đổi.